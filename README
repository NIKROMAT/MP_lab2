Для сборки пропишите  ```make lab2```

Для генерации наборов данных  ```make generate```

Для первых двух пунктов за раз ```make``` или ```make all```

Для получения результатов и создания графиков ```make graph```

Для создания документации ```make doc```

Для очистки от ненужных файлов ```make clean```

Для очистки от большинства восстанавливаемых файлов ```make cleanall```